<!DOCTYPE html>
<html>
<head>
  <title>admin</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= render 'admin/shared/navbar' %>

<div class="container">
  <div class="row-fluid">
    <div class="span9">
      <% unless flash.nil? %>
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <% end %>
      <%= yield %>
    </div>

    <div class="span3">
      <span><%= "Καλωσήλθες, " + current_user.name if logged_in? %></span>
    </div>
  </div>
</div>

<%= debug(params) %>
<%= debug(session) %>
</body>
</html>
