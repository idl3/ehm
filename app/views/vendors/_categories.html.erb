<div class="tabbable tabs-left" id="categories">
  <ul class="nav nav-tabs">

  <li <%= raw 'class="active"' unless params[:c] %>>
    <%= link_to "Όλα τα προϊόντα", @vendor %>
  </li>

  <% vendor_categories.each do |category| %>
  <li <%= raw 'class="active"' if active_category?(category) %>>
    <%= link_to category.title, @vendor.username + "?c=" + category.id.to_s %>
  </li>
  <% end %>

</ul>
</div>